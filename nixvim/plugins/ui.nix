{
  plugins.noice.enable = true;
  plugins.which-key.enable = true;
  plugins.indent-blankline.enable = true;
  plugins.lualine = {
    enable = true;
    globalstatus = true;
  };
  plugins.alpha = {
    enable = true;
  };
}
